[package]
name = "spriterize"
authors = ["Y. D. Santos <yds12@protonmail.com>", "Dmitrii Seniushkin <mr.verein@gmail.com>"]
version = "0.1.2"
edition = "2021"
description = "GUI image editor for pixel art"
homepage = "https://github.com/yds12/tarsila"
repository = "https://github.com/yds12/tarsila"
readme = "README.md"
documentation = "https://docs.rs/tarsila/latest/"
keywords = ["image", "pixel", "pixel-art", "image-editor", "editor"]
categories = ["game-development", "graphics", "multimedia", "multimedia::images", "rendering"]
license = "MIT/Apache-2.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
lapix = { path = "../lapix", version = "0.1.2" }
artlib = { path = "../artlib" }
artlib-script = { path = "../artlib-script" }
egui-macroquad = { version = "0.17.3", default-features = false }
macroquad = "0.4.15"
rfd = "0.10.0"
egui = "0.31.1"
# Pinned to 0.7.x: it targets egui ^0.31, matching our stack. 0.8+ needs egui
# 0.32+, which egui-macroquad/miniquad don't support.
egui-snarl = "0.7.1"
serde = { version = "1.0.152", features = ["derive"] }
thiserror = "1.0.40"
serde_json = "1.0"
zip = { version = "2.2", default-features = false, features = ["deflate"] }
image = { version = "0.24.5", default-features = false, features = ["png"] }

